# PC 界面 v2 原型需求

这份文档记录 2026-03-15 这一轮 PC 桌面端原型改版目标，作为设计稿和后续实现的共同参照。

## 1. 改版背景

- 旧版 PC 原型把玩家信息、功能按钮和说明文案都摊在桌面上，阅读负担偏重。
- mobile 端的思路更接近“先看局面，再看动作”，信息优先级更清晰。
- 这次 PC 改版不改玩法规则，重点只在桌面信息架构和原型布局。

## 2. 本轮目标

- 参考 mobile 的轻量信息结构，减少 PC 长句说明。
- 把不重要或低频的功能收进菜单或按钮，不再长期占用主战场。
- 重新规划 5 个出牌区，让桌面构图更规整、更对称。
- 把头像、身份、等级、剩余牌数、个人分数等关键信息并入出牌区头部。
- 简化独立玩家面板，不再承担“展示手牌”的任务。

## 3. 新版布局原则

### 顶部

- 顶部只保留一行高优先级状态：
- 阶段
- 当前轮到谁
- 主牌 / 级牌
- 打家 / 轮次
- 倒计时
- 非打家总分

- 朋友牌缩成顶部左侧的紧凑模块。
- 规则帮助、Debug、播报、底牌、布局编辑、AI 难度、牌面切换等放进 `更多功能` 菜单。

### 中央战场

- 中央区域作为 PC 第一视觉焦点。
- 5 个玩家出牌区统一为同规格卡片，避免旧版左大右小、上下松紧不一致的问题。
- 每个出牌区头部直接显示：
- 头像
- 玩家名
- 阵营身份
- 等级
- 剩余牌数或个人分数
- 特殊状态，例如 `大`、`庄`、`友`

- 未出牌时显示短空态，例如 `待出`，不再写长说明。
- 中央保留一条焦点提示带，用于关键操作提示和重要播报。

### 玩家面板

- 玩家面板退化成轻量状态列。
- 默认只保留头像、序号、身份或轮转信息。
- 不再在独立玩家面板里展示手牌内容。
- 需要展示的关键状态，优先合并到对应的出牌区头部。

### 底部

- 底部仍保留自己的手牌分组展示。
- 自己的头像、身份和当前回合状态放在手牌区起点，形成从“我是谁”到“我要出什么牌”的连续阅读路径。
- 高频动作继续贴近底部手牌区保留。

## 4. 与 mobile 的对齐点

- 都把公共状态压缩成短标签和短句，不依赖大段说明文案。
- 都强调中央战场的当前局势，而不是边栏堆信息。
- 都优先展示行动相关信息，再展示解释性信息。
- 都把低频功能收纳，不抢主流程注意力。

## 5. 本轮不包含的内容

- 不修改出牌、叫朋友、扣底、计分等玩法逻辑。
- 不同步改 mobile 实现。
- 不直接把这份设计稿完整落到运行态页面。

## 6. 设计稿落点

- 主设计稿：[/Users/mo.li/Documents/cards/artifacts/pc-layout-mockup.svg](/Users/mo.li/Documents/cards/artifacts/pc-layout-mockup.svg)
- 预览封装：[/Users/mo.li/Documents/cards/artifacts/pc-layout-mockup-inline.svg](/Users/mo.li/Documents/cards/artifacts/pc-layout-mockup-inline.svg)

## 7. 后续实现建议

- 先把 PC 运行态里的顶部按钮区拆成 `高频直出 + 更多菜单`。
- 再重做 `trick-spot` 的信息头，把旧 `seat` 面板里的状态迁入出牌区。
- 最后收窄或移除旧版独立玩家大面板，保留轻量身份列即可。
